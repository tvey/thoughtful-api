version: "3.8"

services: 
  api:
    image: thoughtful
    container_name: thoughtful
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    ports: 
      - "8001:8000"
    volumes:
      - .:/app
    env_file:
      - ./.env